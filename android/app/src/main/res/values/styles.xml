<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="colorAccent">#fe6d52</item> <!-- This will also apply to DatePicker unless overridden -->
        <!-- Use Custom DatePicker theme for date picker dialogs -->
        <!-- <item name="android:datePickerDialogTheme">@style/CustomDatePickerTheme</item> -->
    </style>
    <!-- Custom theme for the DatePickerDialog (optional, only apply when needed) -->
    <style name="CustomDatePickerTheme" parent="Theme.AppCompat.Light.Dialog">
        <!-- Customize header color -->
        <item name="colorAccent">#3B2E4A</item> <!-- Optional override -->
        <item name="android:datePickerDialogTheme">@style/CustomDatePicker</item> <!-- Link to CustomDatePicker -->
    </style>

    <!-- Custom DatePicker styling -->
    <style name="CustomDatePicker" parent="android:Widget.Material.DatePicker">
        <item name="android:headerBackground">#0000FF</item> <!-- Header background color -->
        <item name="android:textColorPrimary">#FFFFFF</item> <!-- Header text color -->
        <item name="android:colorAccent">#FF4081</item> <!-- Active date color (for selected date) -->
    </style>

</resources>
